[tool.pytest.ini_options]
pythonpath = ["custom_components"]
asyncio_mode = "auto"

[tool.mypy]
python_version = "3.13"
ignore_missing_imports = true

[tool.pylint.main]
init-hook = "import sys; sys.path.insert(0, 'custom_components')"
